# ========================================
# API Configuration
# ========================================

# Client-side API URL (used by browser)
# This is exposed to the browser, prefix with NEXT_PUBLIC_
NEXT_PUBLIC_API_URL=http://localhost:8081/api

# Server-side API URL (used inside Docker container for SSR)
# This is only used server-side, no NEXT_PUBLIC_ prefix needed
API_URL=http://backend:8080/api


# ========================================
# Google OAuth 2.0 Configuration
# ========================================

# Google OAuth Client ID (Public - exposed to browser)
# Get this from: https://console.cloud.google.com/apis/credentials
# Steps:
#   1. Create a new OAuth 2.0 Client ID
#   2. Set Application Type to "Web application"
#   3. Add authorized JavaScript origin: http://localhost:3001
#   4. No redirect URI needed (using Google Sign-In SDK)
# Example format: 123456789-abcdefghijklmnop.apps.googleusercontent.com
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-google-client-id-here.apps.googleusercontent.com


# ========================================
# Feature Flags (Optional)
# ========================================

# Enable/disable leaderboard feature (Phase 3)
# Set to false until Phase 3 is implemented
NEXT_PUBLIC_ENABLE_LEADERBOARD=false

# Enable/disable assignments feature (Phase 3)
# Set to false until Phase 3 is implemented
NEXT_PUBLIC_ENABLE_ASSIGNMENTS=false


# ========================================
# Setup Instructions
# ========================================
#
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Set up Google OAuth:
#    - Go to https://console.cloud.google.com/apis/credentials
#    - Create OAuth 2.0 Client ID
#    - Add redirect URI: http://localhost:3001/api/auth/callback/google
#    - Copy Client ID and Secret to GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET
#
# 3. Generate NEXTAUTH_SECRET:
#    openssl rand -base64 32
#
# 4. Update GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET with your values
#
# 5. Never commit .env file to git (already in .gitignore)
#
# For detailed setup instructions, see: GOOGLE_OAUTH_SETUP.md
#
